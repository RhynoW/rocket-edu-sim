# Falcon 9-like Rocket Educational Simulator — API dependencies
# Install with:  pip install -r apps/api/requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation (shared with rocket_core)
pydantic>=2.0

# Physics solvers (local package — install from packages/ directory)
# pip install -e packages/rocket_core
# scipy and numpy are transitive deps of rocket_core

# Testing
pytest>=7.4
pytest-asyncio>=0.23
httpx>=0.27          # async test client for FastAPI

# Development / linting (optional)
ruff>=0.4
mypy>=1.10
